---
name: colloidal-packing
description: simulate colloidal packing using `colpack` library, which use Hoomd-Blue to run hard particle Monte Carlo simulations and analyze the results.
compatibility: designed for opencode and alternatives, utilize colpack library, which depends on `hoomd` for simulations, `ovito` for visualization, `freud` and `rowan` for aalysis, and `matplotlib` for plotting.
tools:
license: MIT
metadata:
  author: Lijie Ding
  version: "0.1"
---

# colpack skill

